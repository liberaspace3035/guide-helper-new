stages:
  - deploy

variables:
  DEPLOY_PATH: "/var/www/guide-helper"
  DEPLOY_REPO_URL: "$DEPLOY_REPO_URL"  # GitLab CI/CD Variablesで設定

deploy_production:
  stage: deploy
  image: alpine:latest
  only:
    - main  # または production ブランチ
    - tags  # タグが付けられた場合もデプロイ
  before_script:
    - apk add --no-cache openssh-client git
    - mkdir -p ~/.ssh
    - echo "$DEPLOY_SSH_KEY" > ~/.ssh/deploy_key
    - chmod 600 ~/.ssh/deploy_key
    - ssh-keyscan -p $DEPLOY_PORT $DEPLOY_HOST >> ~/.ssh/known_hosts
  script:
    - |
      ssh -i ~/.ssh/deploy_key -p $DEPLOY_PORT $DEPLOY_USER@$DEPLOY_HOST << 'ENDSSH'
        set -e
        cd $DEPLOY_PATH
        
        echo "🔄 Starting deployment..."
        
        # リポジトリの存在確認と初回クローン
        if [ ! -d .git ]; then
          echo "📦 初回デプロイ: Gitリポジトリをクローンします..."
          
          # ディレクトリが空でない場合は、一時的にバックアップ
          if [ "$(ls -A . 2>/dev/null)" ]; then
            echo "⚠️  ディレクトリにファイルが存在します。一時的にバックアップします..."
            mkdir -p ../backup-$(date +%Y%m%d_%H%M%S)
            mv * ../backup-$(date +%Y%m%d_%H%M%S)/ 2>/dev/null || true
            mv .* ../backup-$(date +%Y%m%d_%H%M%S)/ 2>/dev/null || true
          fi
          
          # リポジトリをクローン（DEPLOY_REPO_URLはGitLab CI/CD Variablesで設定）
          if [ -z "$DEPLOY_REPO_URL" ]; then
            echo "❌ Error: DEPLOY_REPO_URLが設定されていません"
            echo "GitLab CI/CD VariablesでDEPLOY_REPO_URLを設定してください"
            exit 1
          fi
          git clone $DEPLOY_REPO_URL .
          
          # ブランチを確認
          git checkout main || git checkout master
          
          echo "✅ リポジトリのクローンが完了しました"
        else
          # 既存リポジトリの場合
          echo "📋 既存リポジトリを更新します..."
          
          # リモート設定の確認
          echo "📋 Checking remote configuration..."
          git remote -v
          
          # 最新のコードを取得
          echo "📥 Pulling latest code..."
          git fetch origin
          git checkout main || git checkout master
          git pull origin main || git pull origin master
        fi
        
        # 依存関係の更新
        echo "📦 Installing dependencies..."
        composer install --no-dev --optimize-autoloader
        npm install
        npm run build
        
        # マイグレーション実行
        echo "🗄️  Running migrations..."
        php artisan migrate --force
        
        # キャッシュのクリアと再構築
        echo "🧹 Clearing and rebuilding caches..."
        php artisan config:clear
        php artisan route:clear
        php artisan view:clear
        php artisan config:cache
        php artisan route:cache
        php artisan view:cache
        
        # サービスの再起動
        echo "🔄 Restarting services..."
        sudo systemctl restart guide-helper-queue
        sudo systemctl restart guide-helper-scheduler
        
        echo "✅ Deployment completed!"
        echo "📅 Deployed at: $(date)"
        echo "📌 Commit: $(git rev-parse --short HEAD)"
      ENDSSH
  after_script:
    - rm -f ~/.ssh/deploy_key

